// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: mini_llm_serve/v1/execute.proto

package mini_llm_servev1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/qujing226/mini-llm-serve/gen/go/mini_llm_serve/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// ExecuteServiceName is the fully-qualified name of the ExecuteService service.
	ExecuteServiceName = "mini_llm_serve.v1.ExecuteService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// ExecuteServiceExecuteBatchProcedure is the fully-qualified name of the ExecuteService's
	// ExecuteBatch RPC.
	ExecuteServiceExecuteBatchProcedure = "/mini_llm_serve.v1.ExecuteService/ExecuteBatch"
)

// ExecuteServiceClient is a client for the mini_llm_serve.v1.ExecuteService service.
type ExecuteServiceClient interface {
	ExecuteBatch(context.Context, *v1.ExecuteBatchRequest) (*v1.ExecuteBatchResponse, error)
}

// NewExecuteServiceClient constructs a client for the mini_llm_serve.v1.ExecuteService service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewExecuteServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) ExecuteServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	executeServiceMethods := v1.File_mini_llm_serve_v1_execute_proto.Services().ByName("ExecuteService").Methods()
	return &executeServiceClient{
		executeBatch: connect.NewClient[v1.ExecuteBatchRequest, v1.ExecuteBatchResponse](
			httpClient,
			baseURL+ExecuteServiceExecuteBatchProcedure,
			connect.WithSchema(executeServiceMethods.ByName("ExecuteBatch")),
			connect.WithClientOptions(opts...),
		),
	}
}

// executeServiceClient implements ExecuteServiceClient.
type executeServiceClient struct {
	executeBatch *connect.Client[v1.ExecuteBatchRequest, v1.ExecuteBatchResponse]
}

// ExecuteBatch calls mini_llm_serve.v1.ExecuteService.ExecuteBatch.
func (c *executeServiceClient) ExecuteBatch(ctx context.Context, req *v1.ExecuteBatchRequest) (*v1.ExecuteBatchResponse, error) {
	response, err := c.executeBatch.CallUnary(ctx, connect.NewRequest(req))
	if response != nil {
		return response.Msg, err
	}
	return nil, err
}

// ExecuteServiceHandler is an implementation of the mini_llm_serve.v1.ExecuteService service.
type ExecuteServiceHandler interface {
	ExecuteBatch(context.Context, *v1.ExecuteBatchRequest) (*v1.ExecuteBatchResponse, error)
}

// NewExecuteServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewExecuteServiceHandler(svc ExecuteServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	executeServiceMethods := v1.File_mini_llm_serve_v1_execute_proto.Services().ByName("ExecuteService").Methods()
	executeServiceExecuteBatchHandler := connect.NewUnaryHandlerSimple(
		ExecuteServiceExecuteBatchProcedure,
		svc.ExecuteBatch,
		connect.WithSchema(executeServiceMethods.ByName("ExecuteBatch")),
		connect.WithHandlerOptions(opts...),
	)
	return "/mini_llm_serve.v1.ExecuteService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case ExecuteServiceExecuteBatchProcedure:
			executeServiceExecuteBatchHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedExecuteServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedExecuteServiceHandler struct{}

func (UnimplementedExecuteServiceHandler) ExecuteBatch(context.Context, *v1.ExecuteBatchRequest) (*v1.ExecuteBatchResponse, error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("mini_llm_serve.v1.ExecuteService.ExecuteBatch is not implemented"))
}
